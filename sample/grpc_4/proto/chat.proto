syntax = "proto3";

package chat;
option go_package = "./protobuf";

service ChatService {
  // 雙向串流：兩邊都加上 stream
  rpc Chat(stream Message) returns (stream Message);
}

message Message {
  string user = 1;
  string text = 2;
}