syntax = "proto3";

package example.client;

option go_package = "example/client";

message User {
  int64 id = 1;
  string name = 2;
}

message SubscribeUsersRequest {}

service UserService {
  rpc SubscribeUsers(SubscribeUsersRequest) returns (stream User);
}
