syntax = "proto3";

package monitor;
option go_package = "goframe_rpc/proto";

service Monitor {
  // Client Streaming
  rpc PushData(stream DataRequest) returns (DataResponse);
}

message DataRequest {
  string device_id = 1;
  string payload   = 2;
}

message DataResponse {
  int32 count = 1;
}